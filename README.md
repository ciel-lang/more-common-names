
The source file is part of [CIEL](https://github.com/ciel-lang/CIEL).

* we have `append`. We now get `nappend` as an alias of `nconc`.
* `remove` -> `nremove`, an alias for `delete`.

that's all for now o/

